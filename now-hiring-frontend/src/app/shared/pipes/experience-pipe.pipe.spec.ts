import { ExperiencePipePipe } from './experience-pipe.pipe';

describe('ExperiencePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ExperiencePipePipe();
    expect(pipe).toBeTruthy();
  });
});
